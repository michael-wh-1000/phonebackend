{
  "name": "phone-back-end",
  "version": "1.0.0",
  "description": "phone backend",
  "main": "index.js",
  "scripts": {
    "build-ui": "rm -rf dist && cd ../frontend && npm run build && cp -r dist ../backend",
    "dev": "nodemon index.js",
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Michael",
  "license": "MIT",
  "dependencies": {
    "express": "^4.21.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
